interface:
  display_name: "Orchestrate"
  short_description: "Selective Codex worker delegation"
  default_prompt: "Use $orchestrate in Codex to complete this work with the primary agent directing and the best supported worker handling only bounded execution."

policy:
  allow_implicit_invocation: false
