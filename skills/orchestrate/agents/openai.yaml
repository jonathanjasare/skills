interface:
  display_name: "Orchestrate"
  short_description: "Codex-only Sol-to-Luna delegation"
  default_prompt: "Use $orchestrate in Codex to complete this work with Sol directing and Luna handling only bounded execution."

policy:
  allow_implicit_invocation: false
